[package]
name = "loam-scene"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Loam scene composition: typed primitives, scene tree, WGSL emission. Renamed from loam-sdf 2026-05-18; will hold mixed SDF + mesh primitives as the rasterization tier lands."

[dependencies]
loam-math  = { path = "../loam-math" }
loam-shape = { path = "../loam-shape" }
glam.workspace = true
serde.workspace = true
ron.workspace = true
