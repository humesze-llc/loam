[package]
name = "rye-anim"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Presentation-tier animation: easing, dt-springs, and t-driven keyframe tracks + a playhead. Deterministic playback (everything is a function of t); off the Tier-0 simulation determinism path."
