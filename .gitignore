# Rust / Cargo
/target
**/*.rs.bk
Cargo.lock

# IDE / editor / tooling
.claude/
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment / local config
.env
.env.local
.env.*.local

# Merge / backup artifacts
*.orig
*.rej

# Personal devlog — backed up separately (Obsidian)
docs/devlog/

# Personal audit / dev scripts (not shipped; rerun locally)
scripts/

# Demo capture artifacts (rye-app capture module + legacy --capture-{apng,gif} flags)
/captures/
*.apng
*.gif
/out.*

# Trunk wasm build output (per-demo bundles emit into ./dist; rebuild any time)
/dist/
